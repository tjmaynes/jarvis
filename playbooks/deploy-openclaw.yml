---
- name: Deploy OpenClaw servers
  hosts: openclaw_servers
  become: true
  vars_files:
    - ../vars/common.yml
    - ../vars/vault.yml
  roles:
    - openclaw.installer.openclaw
    - debian
    - tailscale
    - mise
    - openclaw
